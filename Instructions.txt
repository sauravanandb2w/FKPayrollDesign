Stage1:1) To create Employee Database consisting of 3 tables(master,weekly_table,monthly_table):
________________________________________________________________________________________________________
	->move inside source Directory
	->then run::javac DatabaseConnection.java -d ClassFiles
	->then run::java -cp "ClassFiles;mysql-connector-java-5.1.49-bin.jar" DatabaseConnection
	_________________________________________________________________________
	Result: All three tables are created on mysql server hosted at 127.0.0.1 at port 3306.

Stage:2)New Employee Entries are welcomed now
_________________________________________________________________________________________________________
	->move inside source Directory
	->then run::javac Employee.java -d ClassFiles
	->then run::java -cp "ClassFiles;mysql-connector-java-5.1.49-bin.jar" employee.Employee
	_________________________________________________________________________
	Result: New Employee Entries are Successfully inserted into database(employee).

Stage:3)Employees willing to leave jobs can do it.

Stage:4)Union as a separate type is implemented 
_________________________________________________________________________________________________________
	->move inside source Directory
	->then run::javac Main.java Union.java UnionPayBehavior.java -d ClassFiles
	->then run::java -cp "ClassFiles;mysql-connector-java-5.1.49-bin.jar" Main
	_________________________________________________________________________
	Main class is temporarily built to check perfect working of Union package java files.
	Strategy pattern is used for design implementation.

Stage:5)