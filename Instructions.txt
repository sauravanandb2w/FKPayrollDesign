Stage:1) To create Employee Database consisting of 3 tables(master,weekly_table,monthly_table):
________________________________________________________________________________________________________
	->move inside source Directory
	->then run::javac DatabaseConnection.java -d ClassFiles
	->then run::java -cp "ClassFiles;mysql-connector-java-5.1.49-bin.jar" DatabaseConnection
	_________________________________________________________________________
	Result: All three tables are created on mysql server hosted at 127.0.0.1 at port 3306.

Stage:2)New Employee Entries are welcomed now
_________________________________________________________________________________________________________
	->move inside source Directory
	->then run::javac Employee.java -d ClassFiles
	->then run::java -cp "ClassFiles;mysql-connector-java-5.1.49-bin.jar" employee.Employee
	_________________________________________________________________________
	Result: New Employee Entries are Successfully inserted into database(employee).

Stage:3)Employees willing to leave jobs can do it.

Stage:4)Union as a separate type is implemented 
_________________________________________________________________________________________________________
	->move inside source Directory
	->then run::javac UnionTester.java Union.java UnionPayBehavior.java -d ClassFiles
	->then run::java -cp "ClassFiles;mysql-connector-java-5.1.49-bin.jar" UnionTester
	_________________________________________________________________________
	UnionTester class is temporarily built to check perfect working of Union package java files.
	Strategy pattern is used for design implementation.

Stage:5)Company as a separate type is Implemented
_________________________________________________________________________________________________________
	->move inside source directory
	->then run::javac CompanyTester.java Company.java CompanyPayBehavior.java -d ClassFiles
	->then run::java -cp "ClassFiles;mysql-connector-java-5.1.49-bin.jar" CompanyTester	
	_________________________________________________________________________
	CompanyTester being the entry point for company related payment stuffs.
	CompanyPayBehavior interface is used.

Stage:6)A Single Entry Point for the project is simulated
__________________________________________________________________________________________________________
	->move inside source directory
	->then run::javac MainPayroll.java *.java -d ClassFiles
	->then run::java -cp "ClassFiles;mysql-connector-java-5.1.49-bin.jar"  MainPayroll
	__________________________________________________________________________
	Result: MainPayroll class file acts as master of whole project
			Featuring 12 differnt options to choose.
			